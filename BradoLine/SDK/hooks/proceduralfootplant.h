#pragma once

void __fastcall hkDoProceduralFootPlant(void* this_pointer, void* edx, void* bone_to_world, void* left_foot_chain, void* right_foot_chain, void* pos) {

	// the function is only called by DoExtraBoneProcessing so there is no need to check for return address
	// returning nothing prevents the "leg shuffling"

	return;
}